#include <stdio.h>
#include <stdlib.h>

int main(void){
    int n, i,d;
    char *ptr;
	int temp;
    char a;

    printf("Enter number of char to store: ");
    scanf("%d",&n);


    ptr = (char*)malloc(n*sizeof(char));

    return 0;
    
}
